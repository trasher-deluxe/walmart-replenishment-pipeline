# Python
__pycache__/
*.py[oc]
*.egg-info/
build/
dist/
wheels/

# Virtual environments
.venv/

# Jupyter
.ipynb_checkpoints/

# VSCode
.vscode/

# Ruff / caches
.ruff_cache/
.mypy_cache/
.pytest_cache/

# OS
.DS_Store

# Heavy/generated artifacts
outputs/*
!outputs/.gitkeep
!outputs/*.json
# Per-run pipeline output: churns every run (MLflow run_id + registered version).
# Regenerated by `python src/pipeline.py`, tracked in mlruns/ and uploaded by CI.
outputs/ml_results.json
!outputs/*.md
*.pkl
*.joblib
*.pt
*.h5

# MLflow local tracking store (regenerable con `python src/pipeline.py`)
mlruns/
